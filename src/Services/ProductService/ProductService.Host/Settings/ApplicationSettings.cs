namespace s3ng.ProductService.Host.Settings
{
    public class ApplicationSettings
    {
        public string ? ConnectionString { get; set; } // Как сделать несколько строк подключения
        public string ? DefaultConnection { get; set; }
    }
}
